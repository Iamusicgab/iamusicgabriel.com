---
const { color, title, place, date, textColor, href } = Astro.props;
---

<a href={href} class="w-full max-w-screen-lg">
	<div
		class={`${color} flex flex-col gap-5
	md:flex-row-reverse justify-between items-center p-5 border-4 border-bblack rounded-xl shadow-[-5px_7px_rgba(0,0,0,1)] transition-all hover:shadow-[-10px_13px_rgba(0,0,0,1)] hover:scale-105`}
	>
		<span class="text-sm md:text-xl font-bold min-w-fit">{date}</span>
		<div class="flex flex-col">
			<span
				class={`text-xl md:text-2xl font-bold text-center ${textColor} w-full md:max-w-screen-sm  md:text-left`}
				>{title}</span
			>
			<span
				class="text-sm font-light text-center md:text-left md:max-w-screen-sm"
				>{place}</span
			>
		</div>
	</div>
</a>
