---
const { color, text, textColor } = Astro.props;
---

<div
	class={`${color} w-full max-w-screen-lg flex justify-center p-5 border-4 rounded-xl drop-shadow-[-5px_7px_rgba(0,0,0,1)]`}
>
	<span class={`text-2xl md:text-4xl font-bold flex text-center ${textColor}`}
		>{text}</span
	>
</div>
